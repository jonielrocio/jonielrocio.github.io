backend:
  name: github
  repo: jonielrocio/jonielrocio.github.io
  branch: master

media_folder: "images/blog"
public_folder: "/images/blog"

collections:
  - name: "blog"
    label: "Blog"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    frontmatter_delimiter: "---"
    default_registration: true
    fields:
      - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
      - {label: "Title", name: "title", widget: "string"}
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    identifier_fields: title
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD", storage_format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false}
      - {label: "Featured Image", name: "image", widget: "image"}
      - {label: "Body", name: "body", widget: "markdown"}
